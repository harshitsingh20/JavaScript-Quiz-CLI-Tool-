const chalkAnimation = require('./');

let str = 'Loading...';
const rainbow = chalkAnimation.rainbow('Loading...');

// Add a new dot every second
setInterval(() => {
	rainbow.replace(str += '.');
}, 1000);
